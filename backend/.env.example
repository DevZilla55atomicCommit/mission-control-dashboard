OPENVIKING_KEY=teamirs-dev-key-2026
MLX_HOST=localhost
WHISPER_STT_PORT=8082           # Whisper STT service for voice input transcription (faster-whisper large-v3-turbo at :8082) — update to remote host if deploying Phase 5 production beyond local defaults status read session exploration earlier workflow A Step 3 continuation task
OPENVIKING_API_URL=http://localhost:1933/v1/chat/completions
OPENVIKING_ACCOUNT=teamirs
OPENVIKING_USER=iris

# MLX Models (Qwen3.5 35B-A3B primary, Aux 9B fallback) — Phase 2 ChatBox Core streaming integration work planning meeting kickoff summary queue dismissal notification command path model settings agents read project context docs session exploration earlier workflow A Step 3 continuation status
MLX_MAIN_MODEL=qwen/qwen3.5-35b-a3b   # Main reasoning chat endpoint at :8081 port found in session exploration findings task update verification meeting report generation .env copying next phase transition planning kickoff summary queue dismissal notification command path model settings agents read project context docs status
MLX_AUX_MODEL=qwen/qwen2.5-7b         # Summary/routing tasks fallback to cheap-model routing per Phase 3 delegation work after agent tasks done earlier in workflow A Step 3 continuation task verification meeting report .env copying next phase transition planning kickoff summary queue dismissal notification command path model settings agents status

# MCP servers (leave defaults for local mode) — external toolsets integration beyond standard hermes profiles session management cron jobs background polling Phase 4 work after agent tasks done verification earlier in workflow A Step 3 continuation task update meeting report generation .env copying next phase transition planning kickoff summary queue dismissal notification command path model settings agents read status
MEMORY_MCP_SERVER=localhost
OPENCLAW_MCP_SERVER=localhost

# ChatBox Core (Phase 2) — SSE streaming endpoint validation after agent tasks done Phase 1 completion verification earlier in workflow A Step 3 continuation task update meeting report generation .env copying next phase transition planning kickoff summary queue dismissal notification command path model settings agents read project context docs status
MAX_AUDIO_FILE_BYTES=50*1024*1024     # Limit voice uploads to ~50MB; larger files will return HTTP 413 per Vision's security audit findings from Phase 2 kickoff planning meeting earlier in workflow A Step 3 continuation task verification .env copying next phase transition planning kickoff summary queue dismissal notification command path model settings agents read status
STT_TIMEOUT_SECONDS=60                 # Whisper transcription timeout; adjust if faster-whisper server at :8082 responds slowly during Phase 4 production testing beyond local defaults status read session exploration earlier workflow A Step 3 continuation task update meeting report generation .env copying next phase transition planning kickoff summary queue dismissal notification command path model settings agents
